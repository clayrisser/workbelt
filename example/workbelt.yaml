includes:
  - '**/workbelt.yaml'

autoinstall: false

systems:
  darwin:
    ggrep: |
      brew install grep
    gsed: |
      brew install gsed

  linux:
    docker: https://get.docker.com
    # docker: |
    #   curl -L https://get.docker.com | sh
    # sudo: true
